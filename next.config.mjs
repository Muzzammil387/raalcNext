/** @type {import('next').NextConfig} */
const nextConfig = {
  // i18n: {
  //   locales: ['en', 'ar', 'ch', 'ru'],
  //   defaultLocale: 'en',
  // },
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
